set mbox_type=Maildir
set folder=$HOME/mail
set spoolfile=+/
set header_cache=~/.hcache
auto_view text/html
macro pager p  <previous-line>
macro pager r <search-opposite>
macro index r <search-opposite>

#set allow_8bit
#set charset=koi8-r
